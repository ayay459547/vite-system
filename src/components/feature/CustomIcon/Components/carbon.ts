import {
  ChartTreemap,
  ChartMultitype,
  ChartMaximum,
  ZoomArea,
  ZoomPan,
  FilterReset
} from '@vicons/carbon'

const carbon: Record<string, any> = {
  ChartTreemap,
  ChartMultitype,
  ChartMaximum,
  ZoomArea,
  ZoomPan,
  FilterReset
}

export default carbon
