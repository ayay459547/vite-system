import {
  ChartTreemap,
  ChartMultitype,
  ChartMaximum,
  ZoomArea,
  ZoomPan,
  FilterReset,
  Time,
  TreeView
} from '@vicons/carbon'

const carbon: Record<string, any> = {
  ChartTreemap,
  ChartMultitype,
  ChartMaximum,
  ZoomArea,
  ZoomPan,
  FilterReset,
  Time,
  TreeView
}

export default carbon
